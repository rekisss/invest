/**
 * K-line fetch is now handled by the Python script at the repo root:
 *   python kline_fetch.py
 *
 * This file is kept as a placeholder. The kline-fetch.yml workflow
 * runs Python directly and no longer invokes this script.
 */
console.error('Use kline_fetch.py instead of this script.')
process.exit(1)
